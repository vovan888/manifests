This is the manifest for building Android for the Asus P750 hardware.

To clone Android with Asus P750 specific changes do:

$ mkdir ~/p750droid
$ cd p750droid
$ repo init -u git://github.com/vovan888/manifests.git
$ repo sync

To build the code:
$ cd ~/p750droid
$ make

Also see instructions here:
http://source.android.com/download
